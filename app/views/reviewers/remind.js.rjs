page << %|
$('#dialog-confirm').attr('title', 'Email Reminder Sent');
$('#dialog-confirm-message').html('An email reminder has been sent out to #{@names}');
$("#dialog-confirm").dialog({
        resizable: false,
        height:240,
        modal: true,
        buttons: {
                Ok: function() {
                        $(this).dialog('close');
                }
        }
});|

